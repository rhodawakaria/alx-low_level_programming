#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: an integer to add to b
 * @b: an integer to add to a
 * Return: Always 0
 */

int add(int a, int b)
{
	return (a + b);
}
